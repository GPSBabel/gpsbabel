language: cpp

matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
      services: docker
      compiler: gcc
    - os: linux
      dist: trusty
      sudo: required
      services: docker
      compiler: clang
    - os: osx
      compiler: clang
      cache:
        directories:
          - $HOME/Qt

install:
  - if [ "${TRAVIS_OS_NAME}" = "osx" ]; then . ./tools/travis_install_osx; fi

script:
  - echo $PWD
  - echo $PATH
  - ls $HOME/Qt
  - if [ "${TRAVIS_OS_NAME}" = "linux" ]; then ./tools/travis_script_linux_docker; fi 
  - if [ "${TRAVIS_OS_NAME}" = "osx" ]; then ./tools/travis_script_osx; fi

after_success:
  - cd ${TRAVIS_BUILD_DIR}
  - if [ "${TRAVIS_OS_NAME}" = "osx" ]; then bash ./tools/uploadtool/upload.sh  gui/objects/GPSBabel-*.dmg; fi

branches:
  except:
    - /^(?i:continuous)$/  # Do not build tags that we create when we upload to GitHub Releases

